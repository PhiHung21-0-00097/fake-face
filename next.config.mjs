/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export",
  // trailingSlash: true,
  // images: {
  //   unoptimized: true,
  // },
  // assetPrefix: "https://demo.thietkewebsite.live",
};

export default nextConfig;
